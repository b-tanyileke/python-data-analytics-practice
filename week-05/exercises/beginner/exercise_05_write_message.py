"""Exercise 5: Write a message to a file

Write a function called write_message that accepts message and filename.
Open the file named by filename for writing, write message followed by one
newline character, and then close the file. Return the number of characters in
message. Do not include the newline in the returned count.

For example, if message is "Welcome" and filename is "message.txt":
- write_message("Welcome", "message.txt") should return 7.
- After the function runs, message.txt should contain exactly:

  ```text
  Welcome
  ```
"""


# Write your function below.
