"""Exercise 2: Sentinel counter

Write a function called count_before_stop that accepts a string. Use a while
loop to count the characters before the first asterisk (`*`). The asterisk is
the sentinel: stop counting as soon as it is found. Do not include the
asterisk in the count.

If the string does not contain an asterisk, count every character.

For example:
- count_before_stop("data*analysis") should return 4.
- count_before_stop("Python") should return 6.
- count_before_stop("*start") should return 0.
"""


# Write your function below.
