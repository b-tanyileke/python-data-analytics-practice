# Think About It: Answers

1. `Data-Analytics` followed by `Ready!Go` on the next line. i.e

    ```text
    Data-Analytics
    Ready!Go
    ```

2. `205`
3. `$8.50`
4. C. `str`
5. B. `number = int("12")`
6. C. `MONTHS_PER_YEAR`
7. B. `price * 0.15`
8. Convert both inputs with `int()` before adding them; otherwise `+` joins two strings.
9. A variable name cannot start with a number. It can start with a letter or underscore
10. Use `print("Price: $" + format(price, ".2f"))`.
