"""
Exercise 8: Temperature-converter module

Import the conversion_tools module from this folder. Write a function called
convert_temperature that accepts a temperature and a scale.

- When scale is "C", return the Fahrenheit result by calling
  conversion_tools.celsius_to_fahrenheit.
- When scale is "F", return the Celsius result by calling
  conversion_tools.fahrenheit_to_celsius.
- For any other scale, return "Invalid scale".

For example, convert_temperature(20, "C") should return 68.0 and
convert_temperature(68, "F") should return 20.0.
"""

import conversion_tools

# Write your function below.
