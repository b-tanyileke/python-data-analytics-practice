"""
Exercise 6: Member order total

Create a named constant called MEMBER_DISCOUNT with the value 0.10.
Write a function called order_total that accepts a unit price, quantity, and a
membership response. The membership response will be either "yes" or "no".

Calculate the subtotal. Apply the 10% discount only when the customer is a
member and the subtotal is at least $100. Return the final total.

For example, order_total(25.0, 5, "yes") should return 112.5.
For the same order with "no", the function should return 125.0.
"""

# Write your function below.
